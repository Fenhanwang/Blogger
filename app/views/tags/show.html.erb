<h1>Articles Taggd with <%= @tag.name %> </h1>	
<ul>
	<% @tag.articles.each do |article| %>
		<li><%= link_to article.title, article_path(article) %></li>
	<% end %>
</ul>

<%= link_to "delete tag", tag_path(@tag), method: :delete, data: {confirm: "Really delete the tag?"} %>
